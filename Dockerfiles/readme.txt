################################################
Available Dockerfiles for Installing Matterport3DSimulator !
################################################

When the installation of Matterport3DSimulator using Docker fails due to network packet loss, the Dockerfiles in that folder can be used to replace the original Dockerfile. The Dockerfiles in this folder have been swapped sources (using NetEase source).